{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "nix-build && result/bin/veriety.test",
            "group": {
                "kind": "test",
                "isDefault": true
            }
        }
    ]
}
